[
  {
    "operation": "NFRegister",
    "path": "/nf-instances/{nfInstanceId}",
    "method": "PUT",
    "input_schema": {
      "$ref": "#/components/schemas/NFProfile"
    },
    "constraints": [
      "The path parameter 'nfInstanceID' is required and must be a unique ID of the NF Instance to register.",
      "The request body must be of type 'application/json' and conform to the 'NFProfile' schema.",
      "The 'nfInstanceId', 'nfType', and 'nfStatus' fields are required in the 'NFProfile'.",
      "At least one of 'fqdn', 'ipv4Addresses', or 'ipv6Addresses' must be present in the 'NFProfile'.",
      "The 'heartBeatTimer' field, if present, must be an integer with a minimum value of 1.",
      "The 'priority' field, if present, must be an integer between 0 and 65535.",
      "The 'capacity' field, if present, must be an integer between 0 and 65535.",
      "The 'load' field, if present, must be an integer between 0 and 100."
    ],
    "depends_on": []
  },
  {
    "operation": "NFDeregister",
    "path": "/nf-instances/{nfInstanceId}",
    "method": "DELETE",
    "input_schema": {},
    "constraints": [
      "The 'nfInstanceID' parameter is required and must be provided in the path.",
      "The 'nfInstanceID' parameter must conform to the schema defined in 'TS29571_CommonData.yaml#/components/schemas/NfInstanceId'."
    ],
    "depends_on": [
      "NFRegister"
    ]
  },
  {
    "operation": "NFProfileRetrieve",
    "path": "/nf-instances/{nfInstanceId}",
    "method": "GET",
    "input_schema": {},
    "constraints": [
      "The 'nfInstanceID' parameter is required and must be provided in the path.",
      "The 'nfInstanceID' parameter must conform to the schema defined in 'TS29571_CommonData.yaml#/components/schemas/NfInstanceId'.",
      "The 'requester-features' parameter is optional and can be provided in the query.",
      "The 'requester-features' parameter must conform to the schema defined in 'TS29571_CommonData.yaml#/components/schemas/SupportedFeatures'."
    ],
    "depends_on": []
  },
  {
    "operation": "NFListRetrieve",
    "path": "/nf-instances",
    "method": "GET",
    "input_schema": {},
    "constraints": [
      "The 'nf-type' query parameter is optional and refers to the type of NF.",
      "The 'limit' query parameter is optional, must be an integer, and has a minimum value of 1.",
      "The 'page-number' query parameter is optional, must be an integer, and has a minimum value of 1.",
      "The 'page-size' query parameter is optional, must be an integer, and has a minimum value of 1."
    ],
    "depends_on": []
  },
  {
    "operation": "NFRegister",
    "path": "/nf-instances/{nfInstanceId}",
    "method": "PUT",
    "input_schema": {
      "$ref": "#/components/schemas/NFProfile"
    },
    "constraints": [
      "The 'nfInstanceID' parameter in the path is required and must be a unique ID of the NF Instance to register.",
      "The request body must be in 'application/json' format and conform to the 'NFProfile' schema.",
      "The 'Content-Encoding' header is optional and described in IETF RFC 9110.",
      "The 'Accept-Encoding' header is optional and described in IETF RFC 9110."
    ],
    "depends_on": []
  },
  {
    "operation": "NFDeregister",
    "path": "/nf-instances/{nfInstanceId}",
    "method": "DELETE",
    "input_schema": {},
    "constraints": [
      "The nfInstanceID parameter is required in the path.",
      "The nfInstanceID must be a unique ID of the NF Instance to deregister.",
      "The nfInstanceID schema is referenced from 'TS29571_CommonData.yaml#/components/schemas/NfInstanceId'."
    ],
    "depends_on": [
      "NFRegister"
    ]
  },
  {
    "operation": "NFStatusSubscribe",
    "path": "/subscriptions",
    "method": "POST",
    "input_schema": {
      "$ref": "#/components/schemas/SubscriptionData"
    },
    "constraints": [
      "The request body must be in JSON format.",
      "The request body must include the 'nfStatusNotificationUri' property.",
      "The 'nfStatusNotificationUri' property must be a string.",
      "The 'sharedDataIds' property, if present, must be an array with at least one item.",
      "The 'reqNotifEvents' property, if present, must be an array with at least one item.",
      "The 'reqSnssais' property, if present, must be an array with at least one item.",
      "The 'reqPerPlmnSnssais' property, if present, must be an array with at least one item.",
      "The 'reqPlmnList' property, if present, must be an array with at least one item.",
      "The 'reqSnpnList' property, if present, must be an array with at least one item.",
      "The 'servingScope' property, if present, must be an array with at least one item.",
      "The 'subscriptionId' property is read-only and must match the pattern '^([0-9]{5,6}-(x3Lf57A:nid=[A-Fa-f0-9]{11}:)?)?[^-]+$'."
    ],
    "depends_on": []
  },
  {
    "operation": "NFStatusNotify",
    "path": "/callback-uri",
    "method": "POST",
    "input_schema": {
      "content": {
        "application/json": {
          "schema": {
            "$ref": "#/components/schemas/NotificationData"
          }
        }
      }
    },
    "constraints": [
      "The request body must contain a 'NotificationData' object.",
      "The 'event' property in 'NotificationData' must be one of the following: 'NF_PROFILE_CHANGED', 'NF_REGISTERED', 'SHARED_DATA_CHANGED'.",
      "If 'event' is 'NF_PROFILE_CHANGED', one of 'nfProfile', 'profileChanges', or 'completeNfProfile' must be present.",
      "If 'event' is 'NF_REGISTERED', one of 'nfProfile' or 'completeNfProfile' must be present.",
      "If 'event' is 'SHARED_DATA_CHANGED', 'sharedDataChanges' must be present."
    ],
    "depends_on": [
      "NFStatusSubscribe"
    ]
  },
  {
    "operation": "NFStatusUnsubscribe",
    "path": "/subscriptions/{subscriptionId}",
    "method": "DELETE",
    "input_schema": {},
    "constraints": [
      "The subscriptionID parameter is required in the path.",
      "The subscriptionID must match the pattern '^([0-9]{5,6}-(x3Lf57A:nid=[A-Fa-f0-9]{11}:)?)?[^-]+$'."
    ],
    "depends_on": [
      "NFStatusSubscribe"
    ]
  },
  {
    "operation": "NFListRetrieval",
    "path": "/nf-instances",
    "method": "GET",
    "input_schema": {},
    "constraints": [
      "The 'nf-type' query parameter is optional and refers to the type of NF.",
      "The 'limit' query parameter is optional and must be an integer with a minimum value of 1.",
      "The 'page-number' query parameter is optional and must be an integer with a minimum value of 1.",
      "The 'page-size' query parameter is optional and must be an integer with a minimum value of 1."
    ],
    "depends_on": []
  },
  {
    "operation": "NFProfileRetrieval",
    "path": "/nf-instances/{nfInstanceId}",
    "method": "GET",
    "input_schema": {},
    "constraints": [
      "The 'nfInstanceID' parameter is required and must be provided in the path.",
      "The 'nfInstanceID' parameter must conform to the schema defined in 'TS29571_CommonData.yaml#/components/schemas/NfInstanceId'.",
      "The 'requester-features' parameter is optional and can be provided in the query.",
      "The 'requester-features' parameter must conform to the schema defined in 'TS29571_CommonData.yaml#/components/schemas/SupportedFeatures'."
    ],
    "depends_on": [
      "NFRegister"
    ]
  }
]